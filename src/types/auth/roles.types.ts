export type role = 'User' | 'Moderator' | 'Administrator';

/**
 * ```typescript
 * interface IRoles {
    user: role;
    moderator: role;
    admin: role;
  }
 * ```
 */
export interface IRoles {
  user: role;
  moderator: role;
  admin: role;
}